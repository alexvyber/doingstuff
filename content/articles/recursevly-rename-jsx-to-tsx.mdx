import { ArticleLayout } from "@/layouts/article-layout"

export const metadata = {
  author: "Alex Vyber",
  date: "2023-01-11",
  title: "Recursively renaming file extensions",
  description: "What if we need to rename all `jsx` files to `tsx`?",
}

export default (props) => <ArticleLayout meta={meta} {...props} />

What if we need to rename all `jsx` files to `tsx`?

## Using ZMV

zmv is a function provided by the ZSH

```shell
 zmv '(**/)(*).jsx' '$1$2.tsx'
```

_to be continued..._
